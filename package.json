{
  "name": "shredsheet",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "The Shredsheet - a smart fat-loss / muscle-gain / recomp tracker with a live AI coach. Rebuilt from the original Excel tool.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:single": "SINGLE=1 vite build --outDir dist-single",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "recharts": "^2.12.7"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.7",
    "vite-plugin-singlefile": "^2.1.0"
  }
}
